const coseDaComprare= ["pane","pasta","carne","uova","latte"]
const container = document.body
let i = 0
while (i < coseDaComprare.lenght) {
    const element = coseDaComprare[i;]
    const lista= document.createElement("p")
    lista.innerHTML = element
    contenitore.append(lista)

    i++;
}